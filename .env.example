# Copy to secret.env (or export directly) — never commit real values.

# Optional: enables the Telegram bot bridge for note-by-text and reminders.
# Get a token from @BotFather in Telegram.
TELEGRAM_BOT_TOKEN=

# Optional overrides
# PORT=3113
# DATA_DIR=./data
